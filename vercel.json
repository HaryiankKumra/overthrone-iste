{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/overthrone run build && rm -rf public && mkdir -p public && SRC=artifacts/overthrone/dist/public && [ -d dist/public ] && SRC=dist/public; cp -R \"$SRC\"/. public/",
  "outputDirectory": "public",
  "functions": {
    "api/[...all].ts": {
      "runtime": "nodejs20.x",
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/[...all].ts"
    },
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}
